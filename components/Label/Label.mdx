import { Meta } from "@storybook/addon-docs/blocks";
import * as LabelStories from "./Label.stories";

<Meta of={LabelStories} />

# Label

Label renders a styled `<label>` element and supports `required` and `invalid` states.
