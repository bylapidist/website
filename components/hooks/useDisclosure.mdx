import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/useDisclosure" />

# useDisclosure

Manages open state for toggleable UI elements and provides `getTriggerProps` and `getPanelProps` to wire accessibility attributes. See the [disclosure policy](../docs/use-disclosure.mdx) for usage guidelines.
