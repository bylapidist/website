import { ArgTypes, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import Layout from "./Layout";
import * as LayoutStories from "./Layout.stories";

<Meta of={LayoutStories} /># Layout

Flex container with configurable gap and orientation.

<Canvas>
    <Story name="example">
        <Layout orientation="horizontal">
            <div>One</div>
            <div>Two</div>
        </Layout>
    </Story>
    <Story name="vertical">
        <Layout>
            <div>One</div>
            <div>Two</div>
        </Layout>
    </Story>
</Canvas>

<ArgTypes of={Layout} />
