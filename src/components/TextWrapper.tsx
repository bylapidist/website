import React from 'react';
import styled from 'styled-components';
import { Text } from '@lapidist/components';

const TextWrapper: React.FC = styled(Text)`
    margin: 0 0 16px;
    line-height: 1.8;
    letter-spacing: 0.8px;
`;

export default TextWrapper;
