import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/usePrefersReducedMotion" />

# usePrefersReducedMotion

Detects whether the user has enabled reduced motion in their operating system preferences using the `prefers-reduced-motion` media query.
